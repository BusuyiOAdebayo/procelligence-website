const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];$('#year').textContent=new Date().getFullYear();addEventListener('scroll',()=>$('#header').classList.toggle('scrolled',scrollY>20));$('#menu').onclick=()=>$('#nav').classList.toggle('open');$$('#nav a').forEach(a=>a.onclick=()=>$('#nav').classList.remove('open'));const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});$$('.reveal').forEach(e=>io.observe(e));
const c=$('#field'),x=c.getContext('2d');let pts=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:Math.min(85,innerWidth/18)},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.14,vy:(Math.random()-.5)*.14}))}function draw(){x.clearRect(0,0,innerWidth,innerHeight);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;x.fillStyle='#69e6ff55';x.beginPath();x.arc(p.x,p.y,1.2,0,7);x.fill()});for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){let d=Math.hypot(pts[i].x-pts[j].x,pts[i].y-pts[j].y);if(d<110){x.strokeStyle=`rgba(94,151,255,${.09*(1-d/110)})`;x.beginPath();x.moveTo(pts[i].x,pts[i].y);x.lineTo(pts[j].x,pts[j].y);x.stroke()}}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();
$$('.node').forEach(n=>n.onmouseenter=()=>$('#nodeInfo').innerHTML=`<b>${n.textContent}</b><span>${n.dataset.text}</span>`);$$('.architecture button').forEach(b=>b.onclick=()=>$('#archInfo').innerHTML=`<b>${b.dataset.title}</b><p>${b.dataset.detail}</p>`);$$('.stage').forEach(s=>s.onclick=()=>{$$('.stage').forEach(q=>q.classList.remove('active'));s.classList.add('active');$('#stageInfo').innerHTML=`<span>${s.dataset.i}</span><div><b>${s.dataset.t}</b><p>${s.dataset.d}</p></div>`});let timer;$('#runDemo').onclick=()=>{clearInterval(timer);let i=0,steps=$$('.step');steps.forEach(s=>s.classList.remove('active'));steps[0].classList.add('active');timer=setInterval(()=>{if(++i>=steps.length)return clearInterval(timer);steps[i].classList.add('active')},650)};
const apps={bio:['01','Biologics manufacturing','Agentic systems for process understanding, optimization, digital twins, and intelligent manufacturing workflows.',['Upstream','Downstream','Drug product','Digital twins','PAT & control','Autonomous workflows']],chem:['02','Chemical and process systems','Reasoning and computational intelligence for reaction engineering, transport, separations, process design, and operations.',['Reaction engineering','Transport','Separations','Process intensification','Control','Autonomous operations']],energy:['03','Energy and sustainability','Scientific agents for complex energy systems, resource efficiency, decarbonization, and next-generation technology.',['Batteries','Hydrogen','Carbon capture','Energy storage','Resource efficiency','System optimization']],materials:['04','Materials intelligence','Computational intelligence for materials discovery, formulation, characterization, structure-property relationships, and scale-up.',['Discovery','Formulation','Characterization','Multiscale modeling','Scale-up','Property optimization']],mfg:['05','Advanced manufacturing','Agentic workflows for equipment intelligence, process design, monitoring, optimization, and adaptive manufacturing.',['Equipment intelligence','Process design','Quality systems','Monitoring','Optimization','Adaptive control']]};$$('.tabs button').forEach(b=>b.onclick=()=>{let a=apps[b.dataset.app];$$('.tabs button').forEach(q=>q.classList.remove('active'));b.classList.add('active');$('#appPanel').innerHTML=`<div><small>${a[0]} / DOMAIN</small><h3>${a[1]}</h3><p>${a[2]}</p></div><div class="topics">${a[3].map(t=>`<span>${t}</span>`).join('')}</div>`});
const form=$('#chatForm'),input=$('#chatInput'),messages=$('#messages');let history=[];function msg(role,text,thinking=false){let d=document.createElement('div');d.className='msg '+role+(thinking?' thinking':'');d.innerHTML=`<small>${role==='user'?'YOU':'PROCELLIGENCE'}</small><p>${text.replace(/</g,'&lt;').replace(/\n/g,'<br>')}</p>`;messages.appendChild(d);messages.scrollTop=messages.scrollHeight;return d}async function ask(text){msg('user',text);history.push({role:'user',content:text});let t=msg('assistant','Reasoning... selecting relevant knowledge and forming a response.',true);try{let r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:history})}),d=await r.json();if(!r.ok)throw Error();t.remove();msg('assistant',d.message);history.push({role:'assistant',content:d.message})}catch(e){t.remove();msg('assistant','The live agent is not connected in this preview. Deploy on Vercel and add OPENAI_API_KEY to enable it. For collaboration, contact contact@procelligence.ai.')}}form.onsubmit=e=>{e.preventDefault();let v=input.value.trim();if(v){input.value='';ask(v)}};$$('.chips button').forEach(b=>b.onclick=()=>ask(b.textContent));$('#clear').onclick=()=>{history=[];messages.innerHTML='<div class="msg assistant"><small>PROCELLIGENCE</small><p>Conversation cleared. What would you like to explore?</p></div>'};
